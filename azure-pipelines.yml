trigger:
- main

pool:
  name: Default

steps:
- script: docker version
  displayName: Docker Version

- script: docker build -t nginx-test .
  displayName: Docker Build

- script: docker run --rm nginx-test nginx -v
  displayName: Docker Run Test